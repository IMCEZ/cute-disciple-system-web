# 「可爱徒弟系统」后端架构蓝图 — 完整设计与实施

> 本文档为后端独立蓝图，与前端蓝图(v2.0)和路程档案配合使用。
> 全程由开发者独立完成，覆盖数据库、API服务、AI网关、业务逻辑全链路。

---

## 目录

### 一、初步蓝图（架构总览）
### 二、大部分（模块划分）
### 三、小部分（子模块拆解）
### 四、步骤级（可实施步骤清单）

---

## 一、初步蓝图（架构总览）

### 1.1 项目定位

轻量级 Node.js 后端服务，为「可爱徒弟系统」前端提供数据持久化、AI 对话生成、业务逻辑运算能力。与前端完全分离，通过 RESTful API + SSE 流式通信。

### 1.2 技术选型

| 层级 | 技术 | 版本 | 理由 |
|------|------|------|------|
| 运行时 | Node.js | >=18 LTS | 事件驱动、非阻塞IO、SSE原生支持 |
| Web框架 | Express.js | 4.x | 最成熟、中间件生态丰富 |
| 数据库 | SQLite (better-sqlite3) | 最新 | 零配置、单文件、同步API性能好 |
| AI SDK | openai 官方包 | 4.x | 原生支持SSE流式 |
| 跨域 | cors | 最新 | 前后端分离必需 |
| 日志 | winston | 3.x | 分级日志、文件轮转 |
| 校验 | zod 或 ajv | 最新 | 请求体Schema校验 |

### 1.3 架构模式

三层架构：Route(路由层) → Service(服务层) → Data(数据层)

```
Client (前端)
    ↓ HTTP/SSE
┌─────────────────────────────────────┐
│          Express Server              │
│  ┌─────────┐  ┌─────────┐           │
│  │  Routes  │→ │ Services│           │
│  │ (路由)   │← │ (服务)  │           │
│  └─────────┘  └─────────┘           │
│                    ↓                 │
│              ┌──────────┐           │
│              │  Data     │           │
│              │ (SQLite)  │           │
│              └──────────┘           │
│  ┌─────────┐  ┌─────────┐           │
│  │  AI网关  │→ │ Adapter │           │
│  │ (统一入口)│  │ 多厂商   │           │
│  └─────────┘  └─────────┘           │
└─────────────────────────────────────┘
    ↓ HTTPS
 OpenAI / Claude / Gemini / 本地模型
```

### 1.4 目录结构（最终形态）

```
backend/
├── package.json
├── .env                          # 环境变量（API Key等敏感信息）
├── .env.example                  # 环境变量模板
├── server.js                     # 入口文件
├── app.js                        # Express应用配置
│
├── config/
│   ├── index.js                  # 统一配置读取（合并.env+默认值）
│   ├── database.js               # SQLite连接配置
│   └── providers.js              # AI厂商配置表
│
├── routes/
│   ├── index.js                  # 路由聚合
│   ├── disciple.routes.js        # /api/disciple/*
│   ├── chat.routes.js            # /api/chat/*
│   ├── system.routes.js          # /api/shop/* /wallet /settings
│   ├── story.routes.js           # /api/story/*
│   └── data.routes.js            # /api/data/* (导出/导入)
│
├── controllers/
│   ├── disciple.controller.js    # 徒弟相关请求处理
│   ├── chat.controller.js        # 对话请求处理（含SSE）
│   ├── system.controller.js      # 系统功能
│   ├── story.controller.js       # 故事/新闻生成
│   └── data.controller.js        # 数据管理
│
├── services/
│   ├── disciple.service.js       # 徒弟业务逻辑（收徒/好感/三维）
│   ├── chat.service.js           # 对话业务逻辑
│   ├── economy.service.js        # 经济系统（充值/返利/消费）
│   ├── npc.service.js            # NPC档案管理（防全知核心）
│   ├── state.service.js          # 状态机（好感/关系/认知/三维推进）
│   └── data.service.js           # 数据导入导出
│
├── ai/
│   ├── gateway.js                # AI网关统一入口
│   ├── adapters/
│   │   ├── openai.adapter.js     # OpenAI Chat Completions
│   │   ├── claude.adapter.js     # Anthropic Messages API
│   │   └── gemini.adapter.js     # Google Generative AI
│   ├── prompt-engine.js          # 提示词引擎（四种模板）
│   └── stream-handler.js         # SSE流式处理工具
│
├── middleware/
│   ├── error-handler.js          # 统一错误处理
│   ├── validate.js               # 请求体校验中间件
│   ├── rate-limit.js             # 频率限制
│   └── logger.js                 # 请求日志
│
├── models/
│   ├── index.js                  # 模型聚合
│   ├── disciple.model.js         # 徒弟数据访问
│   ├── world.model.js            # 世界数据访问
│   ├── npc.model.js              # NPC数据访问
│   ├── item.model.js             # 道具数据访问
│   └── snapshot.model.js         # 快照历史数据访问
│
├── database/
│   ├── init.js                   # 数据库初始化（建表+迁移）
│   ├── schema.sql                # DDL建表语句
│   └── seed.sql                  # 初始数据（默认道具等）
│
├── utils/
│   ├── logger.js                 # winston日志实例
│   ├── validators.js             # 公共校验函数
│   └── helpers.js                # 工具函数（日期/随机/字符串）
│
└── scripts/
    ├── migrate.js                # 数据库迁移脚本
    └── seed.js                   # 数据填充脚本
```

### 1.5 API接口总览（19个接口）

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 徒弟 | POST | /api/disciple | 创建徒弟 |
| 徒弟 | GET | /api/disciple/:id | 获取徒弟 |
| 徒弟 | PATCH | /api/disciple/:id/affection | 好感变化 |
| 徒弟 | PATCH | /api/disciple/:id/growth | 三维更新 |
| 徒弟 | POST | /api/disciple/:id/touch | 触摸 |
| 徒弟 | POST | /api/disciple/:id/gift | 送礼 |
| 对话 | POST | /api/chat | 发送消息（SSE流式） |
| 对话 | GET | /api/chat/history | 获取历史 |
| 对话 | DELETE | /api/chat/message/:id | 删除消息 |
| 商城 | POST | /api/shop/recharge | 充值 |
| 商城 | GET | /api/shop/items | 商品列表 |
| 商城 | POST | /api/shop/buy | 购买 |
| 钱包 | GET | /api/wallet | 钱包信息 |
| 新闻 | GET | /api/news | 新闻列表 |
| 短信 | GET | /api/messages | 短信列表 |
| 设置 | PATCH | /api/settings | 更新设置 |
| 故事 | POST | /api/story/backlog | 生成离线回顾 |
| 故事 | POST | /api/story/news | 生成新闻 |
| 数据 | GET | /api/data/export | 导出 |
| 数据 | POST | /api/data/import | 导入 |
| 数据 | GET | /api/data/state | 获取状态 |

---

## 二、大部分（模块划分）

后端分为 **7 个大部分**：

| 编号 | 大部分 | 职责 |
|------|--------|------|
| M1 | **项目初始化与环境** | package.json、.env、server.js、app.js 启动配置 |
| M2 | **数据库层** | SQLite建表、model层CRUD、数据迁移 |
| M3 | **路由与控制器层** | 5个路由模块 + 对应控制器 |
| M4 | **服务层（业务逻辑）** | 徒弟/经济/NPC/状态机/数据服务 |
| M5 | **AI网关与适配器** | 统一入口 + 三大厂商适配器 + 提示词引擎 |
| M6 | **中间件与安全** | 错误处理、校验、限流、日志 |
| M7 | **工具与脚本** | 日志工具、校验函数、迁移/填充脚本 |

---

## 三、小部分（子模块拆解）

### M1 — 项目初始化与环境（拆3个子模块）

| 子模块 | 内容 |
|--------|------|
| M1-S1 | package.json 创建、依赖安装、npm scripts |
| M1-S2 | .env / .env.example 配置、config/index.js 统一读取 |
| M1-S3 | server.js 入口 + app.js Express配置（中间件挂载） |

### M2 — 数据库层（拆4个子模块）

| 子模块 | 内容 |
|--------|------|
| M2-S1 | schema.sql 全部DDL（save/snapshots/worlds/disciples/npcs/items/messages/news） |
| M2-S2 | database/init.js 初始化 + 自动建表 |
| M2-S3 | models 层：disciple.model / world.model / npc.model / item.model / snapshot.model |
| M2-S4 | seed.sql + scripts/seed.js 初始数据填充 |

### M3 — 路由与控制器层（拆5个子模块）

| 子模块 | 内容 |
|--------|------|
| M3-S1 | routes/index.js 聚合 + disciple.routes + disciple.controller |
| M3-S2 | chat.routes + chat.controller（含SSE流式响应） |
| M3-S3 | system.routes + system.controller（商城/钱包/设置） |
| M3-S4 | story.routes + story.controller（backlog生成/新闻生成） |
| M3-S5 | data.routes + data.controller（导出/导入/获取状态） |

### M4 — 服务层（拆6个子模块）

| 子模块 | 内容 |
|--------|------|
| M4-S1 | disciple.service：收徒逻辑（身份生成+锁定）、好感/三维更新 |
| M4-S2 | economy.service：充值、消费、返利计算 |
| M4-S3 | npc.service：NPC自动建档、记忆存储、知识边界构建 |
| M4-S4 | state.service：好感等级推进、关系阶段判定、认知阶段推进 |
| M4-S5 | chat.service：对话管理、历史存储、提示词组装调度 |
| M4-S6 | data.service：全量导出JSON、导入JSON（覆盖/合并） |

### M5 — AI网关与适配器（拆3个子模块）

| 子模块 | 内容 |
|--------|------|
| M5-S1 | ai/gateway.js 统一入口（路由分发、重试、降级） |
| M5-S2 | ai/adapters/（openai / claude / gemini 三个适配器） |
| M5-S3 | ai/prompt-engine.js 四种模板 + ai/stream-handler.js SSE处理 |

### M6 — 中间件与安全（拆4个子模块）

| 子模块 | 内容 |
|--------|------|
| M6-S1 | middleware/error-handler.js 统一错误格式 |
| M6-S2 | middleware/validate.js 请求体Schema校验 |
| M6-S3 | middleware/rate-limit.js 频率限制 |
| M6-S4 | middleware/logger.js 请求日志 |

### M7 — 工具与脚本（拆3个子模块）

| 子模块 | 内容 |
|--------|------|
| M7-S1 | utils/logger.js winston日志配置 |
| M7-S2 | utils/validators.js + utils/helpers.js |
| M7-S3 | scripts/migrate.js + scripts/seed.js |

---

## 四、步骤级（可实施步骤清单）

共 **27 个步骤**，按实施顺序排列，标注依赖关系。

### 第一阶段：项目骨架（步骤 B01-B04）

**B01 — 创建项目目录与 package.json**
```
路径：backend/package.json
内容：name/version/scripts(dependencies: express cors better-sqlite3 openai winston dotenv zod)
输出：npm init -y -> npm install 全部依赖 -> node_modules 存在
```

**B02 — 环境变量配置**
```
路径：backend/.env + backend/.env.example
内容：PORT=3001, DB_PATH=./data/app.db, OPENAI_API_KEY=, CLAUDE_API_KEY=, GEMINI_API_KEY=
输出：dotenv 可读取到 process.env
```

**B03 — 统一配置读取**
```
路径：backend/config/index.js + backend/config/database.js + backend/config/providers.js
内容：合并.env+默认值，导出 config 对象
输出：require('./config') 可获取全部配置
```

**B04 — Express 应用启动**
```
路径：backend/server.js + backend/app.js
内容：app.js 创建express实例+挂载cors/json解析/静态文件
     server.js 监听端口+启动日志
输出：node server.js -> Listening on :3001
```

### 第二阶段：数据库层（步骤 B05-B08）

**B05 — 编写 DDL 建表 SQL**
```
路径：backend/database/schema.sql
涉及表：
  - save(id, version, state, updated_at)
  - snapshots(id, mid, data, created_at)
  - worlds(id, name, tone, era, data)
  - disciples(id, world_id, data)
  - npcs(id, disciple_id, name, relation, dynamic, personality, memories, knowledge, first_seen)
  - items(id, name, type, description, price, coin_price, tier)
  - messages(id, from_who, content, msg_type, created_at)
  - news(id, title, summary, date, content)
  - chat_history(id, disciple_id, role, content, created_at)
输出：9张表的完整DDL
```

**B06 — 数据库初始化模块**
```
路径：backend/database/init.js
功能：
  - 读取 schema.sql 执行建表
  - 自动检测迁移版本（version表）
  - 返回 db 实例（better-sqlite3）
输出：调用 initDatabase() 后数据库文件 + 全部表就绪
```

**B07 — Model 层实现**
```
路径：backend/models/*.js
包含：disciple.model.js / world.model.js / npc.model.js / item.model.js / snapshot.model.js
每个 model 提供 CRUD 静态方法：
  - findById(id), findAll(), create(data), update(id, data), delete(id)
  - 特化方法：findByWorldId(worldId), findByDiscipleId(discipleId) 等
输出：所有 model 方法可正常操作数据库
```

**B08 — 种子数据填充**
```
路径：backend/database/seed.sql + backend/scripts/seed.js
内容：
  - 默认道具（20+个，分4档：日常/疗愈/功法/逆天）
  - 默认新闻模板（8-10条潜伏期新闻）
  - 默认短信模板（5-6条NPC消息）
输出：运行 seed.js 后数据库包含初始数据
```

### 第三阶段：路由与控制器（步骤 B09-B15）

**B09 — 路由聚合 + 徒弟路由+控制器**
```
路径：backend/routes/index.js
     backend/routes/disciple.routes.js
     backend/controllers/disciple.controller.js
接口：
  POST /api/disciple       → disciple.controller.create
  GET  /api/disciple/:id   → disciple.controller.getById
  PATCH /api/disciple/:id/affection → disciple.controller.updateAffection
  PATCH /api/disciple/:id/growth   → disciple.controller.updateGrowth
  POST /api/disciple/:id/touch     → disciple.controller.touch
  POST /api/disciple/:id/gift      → disciple.controller.gift
输出：6个徒弟接口可接收请求
```

**B10 — 对话路由+控制器（含SSE流式）**
```
路径：backend/routes/chat.routes.js
     backend/controllers/chat.controller.js
接口：
  POST /api/chat        → chat.controller.send（SSE流式）
  GET  /api/chat/history → chat.controller.getHistory
  DELETE /api/chat/message/:id → chat.controller.deleteMessage
SSE实现：
  - res.writeHead(200, { Content-Type: text/event-stream, Connection: keep-alive })
  - 逐 token 写入 data: {"type":"token","content":"..."}


  - 结束写入 data: {"type":"done","stateUpdate":{...}}


输出：对话接口可流式返回
```

**B11 — 系统路由+控制器（商城/钱包/设置）**
```
路径：backend/routes/system.routes.js
     backend/controllers/system.controller.js
接口：
  POST /api/shop/recharge → system.controller.recharge
  GET  /api/shop/items    → system.controller.getItems
  POST /api/shop/buy      → system.controller.buy
  GET  /api/wallet        → system.controller.getWallet
  GET  /api/news          → system.controller.getNews
  GET  /api/messages      → system.controller.getMessages
  PATCH /api/settings     → system.controller.updateSettings
输出：7个系统接口可用
```

**B12 — 故事路由+控制器（第二通道/新闻生成）**
```
路径：backend/routes/story.routes.js
     backend/controllers/story.controller.js
接口：
  POST /api/story/backlog → story.controller.generateBacklog
  POST /api/story/news    → story.controller.generateNews
输出：故事生成接口可用
```

**B13 — 数据路由+控制器（导出/导入/状态）**
```
路径：backend/routes/data.routes.js
     backend/controllers/data.controller.js
接口：
  GET  /api/data/export → data.controller.exportData（返回完整JSON）
  POST /api/data/import → data.controller.importData（接收JSON，覆盖/合并模式）
  GET  /api/data/state  → data.controller.getState
输出：数据管理接口可用
```

**B14 — 路由挂载到 app.js**
```
修改：backend/app.js
内容：app.use('/api', routes) 挂载所有路由
输出：全部接口可通过 localhost:3001/api/* 访问
```

**B15 — API 手动测试**
```
工具：curl 或 Postman 或 Thunder Client
测试清单：
  GET  /api/health → 200 { status: 'ok' }
  POST /api/disciple → 201 { data: {...} }
  GET  /api/disciple/1 → 200 { data: {...} }
  POST /api/chat → SSE stream
  GET  /api/shop/items → 200 { data: [...] }
  GET  /api/data/export → 200 { data: {...} }
输出：所有接口返回正确状态码和格式
```

### 第四阶段：服务层（步骤 B16-B21）

**B16 — 徒弟服务（收徒逻辑+好感/三维）**
```
路径：backend/services/disciple.service.js
核心方法：
  - createDisciple(input) → 校验 → 生成身份 → 写入数据库 → 返回
  - updateAffection(id, delta, reason) → 好感变化 → 检查升级 → 关系阶段判定
  - updateGrowth(id, dimension, delta) → 三维更新 → 检查突破事件
  - touchDisciple(id, type) → 无感/有感触摸 → 好感变化
  - giftDisciple(id, itemId) → 送礼 → 好感变化（视契合度浮动）
依赖：M4-S4 state.service 好感升级判定
输出：完整徒弟业务逻辑
```

**B17 — 经济服务（充值/消费/返利）**
```
路径：backend/services/economy.service.js
核心方法：
  - recharge(amount) → 人民币→点数（汇率表）
  - buyItem(userId, itemId, quantity) → 扣点数→加背包
  - applyRebate(amount, reason) → 返利到余额+记录日志
  - getWallet(userId) → 余额+点数+流水
输出：经济系统完整
```

**B18 — NPC服务（防全知核心）**
```
路径：backend/services/npc.service.js
核心方法：
  - registerNPC(name, relation, dynamic) → 创建/更新NPC档案
  - addMemory(name, content) → 添加记忆
  - getNPCProfile(name) → 获取完整档案
  - buildKnowledgeBoundary(sceneNPCs) → 构建知识边界字符串
  - forgetOldMemories(name, limit) → 遗忘超出上限的旧记忆
输出：NPC自动建档+知识边界
```

**B19 — 状态机服务（好感/关系/认知/三维）**
```
路径：backend/services/state.service.js
核心方法：
  - advanceAffectionLevel(currentLevel, currentValue, delta) → {newLevel, newValue, levelUp}
  - checkRelationStage(affLevel, currentStage) → {newStage, unlocked}
  - advanceCognition(currentStage, triggerEvent) → {newStage, progressed}
  - checkBreakthrough(dimension, progress) → {breakthrough, eventType}
  - getLevelName(level) → 等级名称映射
依赖：无（纯计算，不依赖数据库）
输出：状态机核心算法
```

**B20 — 对话服务（历史管理+提示词组装）**
```
路径：backend/services/chat.service.js
核心方法：
  - saveMessage(discipleId, role, content) → 写入chat_history表
  - getHistory(discipleId, limit) → 读取最近N条
  - deleteMessage(id) → 软删除
  - buildChatPrompt(discipleId, userInput, settings) → 调用prompt-engine组装完整提示词
输出：对话管理+提示词组装
```

**B21 — 数据服务（导出/导入）**
```
路径：backend/services/data.service.js
核心方法：
  - exportFull(snapshot) → 读取全部表→拼接JSON
  - importFull(json, mode) → overwrite:清空后写入 / merge:增量合并
  - createSnapshot(mid) → 快照当前状态
  - restoreSnapshot(mid) → 回溯到指定快照
输出：数据导入导出完整
```

### 第五阶段：AI网关（步骤 B22-B24）

**B22 — AI网关统一入口**
```
路径：backend/ai/gateway.js
核心设计：
  async function generate(messages, options)
    options: { provider, model, params, stream }
    
  流程：
  1. 根据 provider 选择适配器
  2. 调用适配器的 generate 方法
  3. 失败时降级（同厂商→跨厂商→mock）
  4. 返回 AsyncGenerator（流式）或完整响应
  5. 每个chunk格式: { type: 'token', content: '...' } 或 { type: 'done', ... }
输出：统一AI调用入口
```

**B23 — 三大厂商适配器**
```
路径：
  backend/ai/adapters/openai.adapter.js
  backend/ai/adapters/claude.adapter.js  
  backend/ai/adapters/gemini.adapter.js

每个适配器实现：
  async function generate(messages, options) {
    // 1. 将通用messages格式转换为厂商特有格式
    // 2. 调用fetch/官方SDK
    // 3. 支持stream: true 返回AsyncGenerator
    // 4. 统一输出chunk格式
  }

OpenAI适配器关键代码：
  const url = `${baseUrl}/chat/completions`
  const body = { model, messages, temperature, max_tokens, stream: true }
  const response = await fetch(url, { method:'POST', headers:{ Authorization: `Bearer ${key}` }, body: JSON.stringify(body) })
  const reader = response.body.getReader()
  // 解析 SSE 行: data: {...}
  // yield { type: 'token', content: delta }

Claude适配器关键差异：
  url = `${baseUrl}/messages`
  body = { model, max_tokens, messages, stream: true }
  // Anthropic SSE 格式: data: [{"type":"content_block_delta","delta":{"text":"..."}}]

Gemini适配器关键差异：
  url = `${baseUrl}/models/${model}:streamGenerateContent`
  body = { contents: [...], generationConfig: {...} }
  // SSE 格式: data: {"candidates":[{...}]}
输出：三大协议完全适配
```

**B24 — 提示词引擎 + SSE处理器**
```
路径：
  backend/ai/prompt-engine.js
  backend/ai/stream-handler.js

prompt-engine.js：
  - buildNarrativePrompt(state, userInput) → 叙事模板
  - buildGreetingPrompt(input) → 收徒模板
  - buildBacklogPrompt(state) → 离线回顾模板
  - buildNewsPrompt(worldStage) → 新闻模板
  每种模板注入：当前状态、设置参数、NPC边界、NSFW标记、人称设定

stream-handler.js：
  - sendSSEHeaders(res) → 设置response头
  - writeSSEChunk(res, chunk) → 写入单个token
  - endSSEStream(res, stateUpdate) → 结束流+状态更新
  - handleError(res, error) → 错误处理
输出：提示词组装+SSE工具
```

### 第六阶段：中间件与安全（步骤 B25-B26）

**B25 — 中间件实现**
```
路径：
  backend/middleware/error-handler.js
  backend/middleware/validate.js
  backend/middleware/rate-limit.js
  backend/middleware/logger.js

error-handler：
  - 捕获所有未处理错误
  - 统一格式: { error: { code, message, details } }
  - 生产环境不暴露堆栈

validate：
  - 使用 zod 定义请求体schema
  - 中间件工厂: validate(schema) → 校验req.body
  - 失败返回 400 + 详细错误

rate-limit：
  - 基于内存计数器（首版简单实现）
  - 每个IP每分钟100次
  - 超出返回 429

logger：
  - 记录 method, url, status, duration
  - 使用 winston 输出到 console + 文件
输出：4个中间件挂载到 Express
```

**B26 — 中间件挂载 + 全局错误处理**
```
修改：backend/app.js
挂载顺序：
  1. logger 中间件（最先）
  2. cors
  3. json parser
  4. rate-limit
  5. 路由
  6. 404 handler（最后）
  7. error-handler（最后）
输出：全部中间件正确工作
```

### 第七阶段：工具与脚本（步骤 B27）

**B27 — 工具函数 + 迁移/填充脚本**
```
路径：
  backend/utils/logger.js → winston实例，按天滚动
  backend/utils/validators.js → isValidAge(), isValidEmotion(), sanitizeString()
  backend/utils/helpers.js → formatDate(), randomId(), deepClone()
  backend/scripts/migrate.js → 读取schema.sql执行迁移
  backend/scripts/seed.js → 填充初始数据
输出：工具函数+脚本可用
```

---

## 五、后端步骤与前端阶段整合

将后端27个步骤分配到8个迭代阶段中：

| 迭代 | 阶段名称 | 前端步骤 | 后端步骤 | 产出 |
|------|---------|---------|---------|------|
| v0.1 | 骨架搭建 | 1-6 | **B01-B04** | 前端三栏 + **后端Express启动** |
| v0.2 | 数据引擎 | 7-10 | **B05-B08** | 前端状态管理 + **数据库+Model层** |
| v0.3 | 中央叙事 | 11-14 | — | 前端消息流+打字机 |
| v0.4 | 左侧面板 | 15-23 | — | 前端8模块弹窗 |
| v0.5 | 右侧手机 | 24-31 | — | 前端7App弹窗 |
| v0.6 | 设置全屏 | 32-35 | **B09-B15** | 前端设置 + **路由+控制器+API可用** |
| v0.7 | AI接入 | 36-41 | **B16-B24** | 前端AI调用 + **服务层+AI网关+三大协议** |
| v0.8 | 高级+测试 | 42-45 | **B25-B27** | 前端NPC隔离 + **中间件+工具+全流程测试** |

### 整合后的完整迭代计划

```
v0.1 (前1-6 + 后B01-B04) → 前端三栏 + 后端Express启动 + /api/health ✅
v0.2 (前7-10 + 后B05-B08) → 前端状态 + 数据库9表 + Model层 + 种子数据 ✅
v0.3 (前11-14)             → 前端消息流+打字机 ✅
v0.4 (前15-23)             → 前端8模块弹窗 ✅
v0.5 (前24-31)             → 前端7App弹窗 ✅
v0.6 (前32-35 + 后B09-B15) → 前端设置全屏 + 19个API接口全部可用 ✅
v0.7 (前36-41 + 后B16-B24) → 前端AI调用 + 完整服务层 + 三大协议AI网关 ✅
v0.8 (前42-45 + 后B25-B27) → 前端高级功能 + 中间件 + 工具脚本 + 全流程通过 ✅
```

---

## 六、关键设计决策

### 6.1 SQLite 优于 JSON 文件
- 结构化查询、事务支持、并发安全
- better-sqlite3 同步API避免回调地狱
- 单文件存储，方便备份和迁移

### 6.2 不封装 ORM
- 直接使用 better-sqlite3 的 prepared statement
- 理由：表结构简单（9张），ORM增加不必要复杂度
- 每个 model 文件封装对应表的 CRUD

### 6.3 适配器模式管理多厂商AI
- 统一接口 + 多适配器，新增厂商只需加一个文件
- 降级链：主厂商→同厂商降级→跨厂商降级→mock

### 6.4 SSE 流式优于 WebSocket
- 单向推送（服务器→客户端）足够
- SSE 基于 HTTP，无需额外握手
- 前端 EventSource 原生支持

### 6.5 NPC隔离方案
- 每个 NPC 独立档案存储在 npcs 表
- API请求时附带当前场景NPC列表
- prompt-engine 根据列表注入对应NPC的知识边界
- 防止 AI 越界知晓 NPC 不该知道的信息

---

> **本后端蓝图包含：1个初步蓝图 → 7个大部分 → 28个子模块 → 27个可实施步骤。**
> 
> **已完成与前端8阶段整合。确认后开始 B01 实施。**
